# filepath: d:\Development\WebDevApps\sms-codingProject\myconvertertool.com\.github\workflows\ci.yml
name: CI
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm install
      - run: npm test
      - run: npm run lint